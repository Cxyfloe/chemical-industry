# 项目结构 (Project Structure)

> 化学工业模组 v0.7.0 — 2026-07-31
>
> **Create 生态参考源码**：
> - `Create-mc1.21.1-6.0.9/` — Create 核心（FluidTank 多方块模式、动能系统）
> - `create craft & additions/src/` — CA（ModularAccumulator、电力系统）
> - `Create-Diesel-Generators-/src/` — DG（DistillationTank 分馏塔、Controller 模式）

---

## 一、Java 源代码

```
src/main/java/com/cxy/chemical_industry/
├── ChemicalIndustry.java                    # ⭐ 主入口（@Mod + 能力注册 + Ponder 注册）
│
├── block/                                   # 自定义方块类
│   ├── AirCompressorBlock.java              #   空气压缩机（DirectionalKineticBlock，齿轮驱动）
│   ├── ChemicalLiquidBlock.java             #   化学流体（反应+腐蚀+中毒+酸入水成就）
│   ├── CondenserPipeBlock.java              #   冷凝管（FACING，鼓风机透风）
│   ├── ElectrolyzerBlock.java               #   电解槽（3×N 深，PART+FACING）
│   ├── FluidizedBedBlock.java               #   沸腾炉（3层 LAYER+LIT，扳手还原）
│   ├── OpiumPoppyBlock.java                 #   罂粟作物（CropBlock，4 阶段）
│   └── ReactiveMetalBlock.java              #   活泼金属块（铝/镁遇酸腐蚀）
│
├── block_entity/                            # 方块实体（Controller 模式兼容 IMultiBlockEntityContainer）
│   ├── AirCompressorBlockEntity.java        #   空气压缩机（KineticBlockEntity + FluidTank）
│   ├── CondenserPipeBlockEntity.java        #   冷凝管（鼓风机依赖、4s 结冰、冻伤）
│   ├── ElectrolyzerBlockEntity.java         #   电解槽（10 配方、findFront 委托、深度缩放）
│   └── FluidizedBedBlockEntity.java         #   沸腾炉（Controller 模式：7罐共享、BFS、层容量平分）
│
├── effect/                                  # 自定义状态效果
│   └── CorrosionEffect.java                 #   腐蚀（分级：护甲损伤+扣血+双手检测）
│
├── fluid/                                   # 流体类型定义
│   ├── ChemicalFluid.java                   #   通用化学流体（Source + Flowing）
│   └── ChemicalFluidType.java               #   自定义贴图渲染
│
├── item/                                    # 自定义物品类
│   ├── FluidizedBedControllerItem.java      #   沸腾炉控制器（右键转化 Create 储罐柱）
│   ├── GasCanisterItem.java                 #   储气罐（支持 9 种气体 + 稀有气体成就）
│   ├── OpiumItem.java                       #   鸦片基类（两段式效果：正面→负面）
│   ├── OpiumPoppyFruitItem.java             #   罂粟果实（种子+食物）
│   └── TooltipBlockItem.java                #   带 Shift 提示的方块物品
│
├── screen/                                  # 机器 GUI
│   ├── FluidizedBedMenu.java                #   沸腾炉（3 槽 GUI + quickMove）
│   ├── FluidizedBedScreen.java              #   沸腾炉界面（进度条、诊断）
│   ├── ElectrolyzerMenu.java                #   电解槽（5 槽：阴/阳/催化/膜/固）
│   └── ElectrolyzerScreen.java              #   电解槽界面（中文标签、进度条）
│
├── event/                                   # 事件处理
│   ├── AdvancementHelper.java               #   成就授予（grantNearby/grantPlayer + 联动检查）
│   ├── DangerousReactionHandler.java        #   钠锭+酸 → 爆炸
│   ├── DistillationSafetyHandler.java       #   分馏塔热源爆炸保护
│   ├── GasLeakHandler.java                  #   管道气体泄漏检测（压力+冷却）+混合爆炸
│   ├── OpiumHandler.java                    #   鸦片副作用计时器 + 工作盆成就扫描
│   └── PlantHandler.java                    #   催熟作物成就
│
├── ponder/                                  # Ponder 教程（详见 .claude/skills/ponder-dev/）
│   ├── ChemicalPonderPlugin.java            #   PonderPlugin 实现（注册场景）
│   └── scenes/
│       └── ElectrolyzerScenes.java          #   电解槽 6 幕剧本（搭建/动能/通电/电极/加热/爆炸）
│
└── registry/                                # 注册中心（DeferredRegister 模式）
    ├── ModBlocks.java                       #   方块注册（11 矿石+深层+机器+金属块+罂粟）
    ├── ModItems.java                        #   物品注册（70+ 含盐类/有机/贵金属/工具盔甲/朱砂/鸦片）
    ├── ModFluids.java                       #   流体注册（21种：溶液+酸+水银+有机液体+9 气体）
    ├── ModBlockEntities.java                #   方块实体类型注册
    ├── ModMenus.java                        #   容器菜单类型注册
    ├── ModEffects.java                      #   状态效果注册
    ├── ModMaterials.java                    #   工具/盔甲材料（铝/钢/硬铝/青铜）
    └── ModCreativeTabs.java                 #   创造标签页（3 个：装置/无机/有机）
```

---

## 二、架构设计：Create Controller 模式

沸腾炉采用与 Create FluidTank / DG DistillationTank 一致的多方块模式：

```
所有沸腾炉方块是同一类型
    │
    ├── 其中一个成为 Controller（有 originalTankKey 的原始中层）
    │       └── 持有所有共享槽（5个FluidTank + 5个ItemStack）
    │
    └── 其余方块 delegrate 到 Controller
            ├── GUI → guiBE() → controller
            ├── 流体 I/O → handlerForCapability(side) → controller.getFluidHandler(side)
            └── 物品 I/O → guiBE().items
```

| 方法 | 作用 |
|------|------|
| `isController()` | `controller==null` = 自己是 controller |
| `getControllerBE()` | 返回 controller BE，或自己 |
| `updateConnectivity()` | BFS 扫描所有相连方块，统一设 controller |
| `handlerForCapability(side)` | 非 controller 递归委托到 controller |
| `sharedWater/H2/N2/Acid/Gas()` | 所有槽访问走 controller |

### 与 Create 源码的对应

| Create | 本项目 |
|--------|--------|
| `IMultiBlockEntityContainer.Fluid` | 5 个独立槽（接口假设单槽，我们有多槽） |
| `ConnectivityHandler.formMulti()` | `updateConnectivity()` BFS |
| `getControllerBE().handlerForCapability()` | `handlerForCapability(side)` |
| `controller` BlockPos 字段 | 相同 |

---

## 三、资源文件速查

### 新增内容需要修改的文件清单

| 新增类型 | 必须修改 |
|---------|---------|
| **固体物品** | `ModItems.java` + `ModCreativeTabs.java` + `models/item/` + `textures/item/` + `zh_cn.json` + `en_us.json` |
| **液体流体** | `ModFluids.java`(registerFluid) + `blockstates/` + `models/block/` + `textures/block/_still+_flow` + `lang`×2 + `fluid/water.json` + `ModCreativeTabs.java`(bucket) |
| **气体流体** | `ModFluids.java`(registerGas) + `textures/block/_still+_flow` + `lang`×2 |
| **机器方块** | `block/XxxBlock.java` + `block_entity/XxxBlockEntity.java` + `ModBlocks.java` + `ModBlockEntities.java` + `ModItems.java`(BlockItem) + `ModCreativeTabs.java` + `blockstates/` + `models/block/` + `models/item/` + `textures/block/` + `loot_table/` + `pickaxe.json` + `needs_stone_tool.json` + `lang`×2 |
| **作物方块** | `block/XxxCropBlock.java`(继承 CropBlock) + `ModBlocks.java` + `ModItems.java`(种子物品+useOn 种植) + `blockstates/`(age 状态) + `models/block/`(4 阶段) + `loot_table/`(age 条件) + `lang`×2 |
| **配方** | `data/chemical_industry/recipe/` 新建 JSON |
| **成就** | `data/chemical_industry/advancement/` 新建 JSON + `AdvancementHelper.java`(代码触发 + 加入 ALL_ACHIEVEMENTS) + `lang`×2 |
| **Ponder 场景** | `assets/<modid>/ponder/<路径>.nbt`(蓝图) + `ponder/scenes/`(场景类) + `lang`×2(翻译 key `<modid>.ponder.<title>.text_N`) |

### Create 搅拌配方格式

```json
{
  "type": "create:mixing",
  "ingredients": [
    {"item": "mod:item"},
    {"type": "fluid_stack", "fluid": "mod:fluid", "amount": 1000}
  ],
  "results": [
    {"id": "mod:item", "amount": 1},
    {"id": "mod:fluid", "amount": 1000}
  ],
  "heatRequirement": "heated"
}
```

### DG 蒸馏配方格式

```json
{
  "type": "createdieselgenerators:distillation",
  "ingredients": [{"type": "fluid_stack", "fluid": "x", "amount": 200}],
  "processing_time": 100,
  "results": [
    {"id": "x", "amount": 210},
    {"id": "y", "amount": 780}
  ]
}
```

---

## 四、依赖模组

| 文件 | 版本 | 作用 |
|------|------|------|
| `create-1.21.1-6.0.9.jar` | 6.0.9 | Create 核心（流体管道、动能、反应釜） |
| `createaddition-1.5.10.jar` | 1.5.10 | CA（电线连接器、FE 供电） |
| `createdieselgenerators-1.21.1-1.3.8.jar` | 1.3.8 | DG（分馏塔、发电） |
| `ponder-neoforge-1.0.87+mc1.21.1` | 1.0.87 | Ponder API（KineticBlockEntity 依赖） |

### 编译依赖（build.gradle）

```groovy
dependencies {
    implementation "net.neoforged:neoforge:${neo_version}"
    implementation files('libs/[机械动力] create-1.21.1-6.0.9.jar')
    implementation "net.createmod.ponder:ponder-neoforge:1.0.87+mc1.21.1"
    implementation files('libs/[机械动力：柴油动力] createdieselgenerators-1.21.1-1.3.8.jar')
}
```

---

## 五、编译与部署

```bash
./gradlew.bat build
cp build/libs/chemical_industry-0.1.0.jar "D:/MC/.minecraft/versions/1.21.1-NeoForge-test/mods/"
```

---

## 六、美术开发指南（如何做正式美术）

> 当前状态：全部方块/物品/流体用的是 `generate_textures.js` 生成的**纯色占位贴图**。
> 正式美术按下面流程**逐个**替换，替换前功能照常能跑（先功能后美术）。

### 6.1 美术流程总览（5 步）

```
① 画贴图 → ② 方块模型 JSON → ③ blockstate → ④ 物品模型 → ⑤ 重新编译
```

三个概念先分清：
- **贴图**（PNG 图片）：画在方块/物品表面的"皮"，只存图片
- **模型**（JSON）：决定方块长什么样（六面立方体 / 自定义形状），引用的贴图路径
- **blockstate**（JSON）：决定方块**什么状态**（facing/part/layer）显示**哪个模型**

### 6.2 文件位置一览

| 内容 | 路径 | 说明 |
|------|------|------|
| 方块贴图 | `assets/chemical_industry/textures/block/` | 如 `sulfur_ore.png` |
| 物品贴图 | `assets/chemical_industry/textures/item/` | 如 `sulfur_dust.png` |
| 流体贴图 | `textures/block/<名>_still.png` + `<名>_flow.png` | 静止面 + 流动面，缺一不可 |
| 方块模型 | `assets/chemical_industry/models/block/` | 方块 3D 外观 |
| 物品模型 | `assets/chemical_industry/models/item/` | 物品栏/手持外观 |
| blockstate | `assets/chemical_industry/blockstates/` | 状态 → 模型映射 |

### 6.3 贴图要求

- PNG 格式，透明背景，**16×16 像素**（原版风格；大机器可用 32×32）
- 文件命名 = 方块/物品注册名（全小写 + 下划线，如 `electrolyzer_center.png`）
- 画图工具：**Aseprite**（像素画专用，动画方便）/ **GIMP**（免费）/ Blockbench 自带画板

### 6.4 方块模型 JSON（三种常用写法）

**① 六面同一贴图**（矿石、金属块等简单方块）：

```json
{
  "parent": "minecraft:block/cube_all",
  "textures": { "all": "chemical_industry:block/sulfur_ore" }
}
```

**② 上下面与侧面不同**（类似原版泥土）：

```json
{
  "parent": "minecraft:block/cube_bottom_top",
  "textures": {
    "bottom": "chemical_industry:block/xxx_bottom",
    "top": "chemical_industry:block/xxx_top",
    "side": "chemical_industry:block/xxx_side"
  }
}
```

**③ 自定义形状**（机器方块、导管等）：用 **Blockbench** 建模 → 导出 JSON → 放进 `models/block/`，把纹理路径改成自己的贴图。

> 贴图路径写法：`<modid>:block/<文件名>`（不带 .png 后缀）

### 6.5 blockstate（状态 → 模型映射）

每个方块一个文件：`blockstates/<注册名>.json`

```json
{
  "variants": {
    "part=center":   { "model": "chemical_industry:block/electrolyzer_center" },
    "part=anode":    { "model": "chemical_industry:block/electrolyzer_anode" },
    "part=cathode":  { "model": "chemical_industry:block/electrolyzer_cathode" }
  }
}
```

- 机器方块有 `facing` / `part` / `layer` 等多个属性时，**每个状态组合都要写一条**（用 `facing=west,part=center` 这种逗号组合）
- 所有属性组合都写全，否则某些状态下方块会显示成黑紫格子（缺失模型）

### 6.6 物品模型

- **方块物品**（拿在手里的方块）：直接复用方块模型：
  ```json
  { "parent": "chemical_industry:block/sulfur_ore" }
  ```
- **普通物品**（扁平面贴图，如粉末、锭）：
  ```json
  {
    "parent": "minecraft:item/generated",
    "textures": { "layer0": "chemical_industry:item/sulfur_dust" }
  }
  ```

### 6.7 快速预览技巧（不用重新编译！）

把美术文件放进**资源包**文件夹，游戏里加载该资源包就能即时预览，调好再复制回 `src/main/resources`：

```
D:/MC/.minecraft/versions/1.21.1-NeoForge-test/resourcepacks/<资源包名>/
  └── assets/chemical_industry/    ← 结构与 src/main/resources/assets 完全一致
```

优点：改一张图 → 游戏内 F3+T 刷新资源包 → 立刻看到效果，省去每次编译几分钟的等待。

### 6.8 推荐工具

| 工具 | 用途 | 说明 |
|------|------|------|
| **Blockbench** | 3D 建模 | Minecraft 建模事实标准，直接导出 JSON 模型 |
| **Aseprite** | 像素画 | 画贴图/动画最顺手（付费，有试用） |
| **GIMP** | 图像处理 | 免费开源的 Photoshop 替代 |
| `generate_textures.js` | 占位贴图 | 项目自带：纯色方块快速生成（先用它跑通功能） |

### 6.9 替换贴图检查清单（每做一个方块）

- [ ] 贴图：`textures/block/` 里 PNG 画好（16×16）
- [ ] 模型：`models/block/` 写 JSON（或 Blockbench 导出）
- [ ] blockstate：`blockstates/` 所有状态组合覆盖全
- [ ] 物品模型：`models/item/` 引用方块模型（机器）或 generated（物品）
- [ ] 流体：`_still.png` + `_flow.png` 两张
- [ ] 重新编译 → 游戏里确认无黑紫格 / 无模型缺失
